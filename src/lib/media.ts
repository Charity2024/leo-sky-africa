import { mediaPaths } from "@/content/site";

/** Returns true when the asset path points to a gallery placeholder image. */
export function isGalleryPlaceholder(src: string): boolean {
  return false;
}
